runs:
  using: composite
  steps:
  - id: output-value
    shell: bash
    run: |
      echo "depth2=depth2" >>$GITHUB_OUTPUT
      echo "depth2=depth2" >>$GITHUB_ENV
  - uses: redgarb/local-post-steps-bug/depth3-absolut@master
    id: depth2
  - id: debug-outputs
    shell: bash
    run: |
      echo "Should be depth2 -> ${{ steps.output-value.outputs.depth2 }}"
      echo "Should be depth2 -> ${{ env.depth2 }}"
      echo "Should be depth3 -> ${{ steps.depth2.outputs.depth3 }}"
      echo "Should be depth3 -> ${{ env.depth3 }}"
      echo "Should be depth4 -> ${{ steps.depth2.outputs.depth4 }}"
      echo "Should be depth4 -> ${{ env.depth4 }}"
  - uses: redgarb/local-post-steps-bug/post-step@master
    with:
      STEPS: ${{ tojson(steps) }}
