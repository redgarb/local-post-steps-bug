runs:
  using: composite
  steps:
  - id: output-value
    shell: bash
    run: |
      echo "depth3=depth3" >>$GITHUB_OUTPUT
  - uses: redgarb/local-post-steps-bug/depth4-absolut@master
    id: depth3
  - uses: redgarb/local-post-steps-bug/post-step@master
    with:
      STEPS: ${{ tojson(steps) }}
