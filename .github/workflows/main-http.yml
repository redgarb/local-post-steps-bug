on:
  push:
  workflow_dispatch:
jobs:
  local-post-steps-bug:
    runs-on: ubuntu-latest
    steps:
    - uses: redgarb/local-post-steps-bug/.github/actions/depth1-absolut@master
    - uses: redgarb/local-post-steps-bug/.github/actions/post-step@master
      with:
        STEPS: ${{ tojson(steps) }}
